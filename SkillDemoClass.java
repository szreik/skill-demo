public class SkillDemoClass{
    // Given two strings, returns one string with space separating 
    // the two given strings. 
    public static String combineStrings(String s1, String s2){
        return s1 + " " +s2;
    }
}